__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
*.so

# bench-output dirs (e.g. MAX_NIC_PER_PE sweep)
maxnic_sweep_*/
*.log
# ...except the raw run logs published as evidence under results/, which are the
# only way to re-check a table without re-renting the hardware.
!**/results/**/*.log

.DS_Store

# EFA installer tarballs: ~650 MB, fetched by build_image.sh into the build context
*.tar.gz
